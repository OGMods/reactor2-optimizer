# Prettier's own defaults cover node_modules; the rest is this repo's.
#
# Note this file uses .gitignore syntax: a `#` only starts a comment at the
# beginning of a line, never after a pattern.

# Build output.
dist/
dist-ssr/

# Generated data, written by something other than a person. Formatting it
# would be undone by the next regeneration and show up as a diff every time.
# packages/solver/fixtures/ is written by the solver's fixture exporter; the
# atlas is packed by an external script that is not part of this repo.
package-lock.json
packages/solver/fixtures/
public/data/web_atlas.json

# Local tooling (gitignored) and generated output.
tools/
solves/
